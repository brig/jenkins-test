pipeline {
    agent { label 'master' }
    stages {
        stage('build') {
        steps {
                echo "Hello World!"
                sleep 30
                echo "After sleep"
            }
        }
    }
}
